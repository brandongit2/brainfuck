# Brandon's Brainfuck interpreter

**Usage:** `node index.js [program]`, where `[program]` is the number of the program you want to execute. The programs
are listed in the `programs` array of `index.js`.
